import React from "react";
import Hero from "../Hero/Hero";
import { Header } from "./Landing.elements";

const Landing = () => {
  return (
    <Header>
      <Hero />
    </Header>
  );
};

export default Landing;
